using System.Windows;
using LamaWorldsApp.ViewModels;

namespace LamaWorldsApp
{
    public partial class MainWindow : Window
    {
        public MainWindow()
        {
            InitializeComponent();
        }
    }
}
